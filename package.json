{
  "name": "manjaro-media3",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "encrypt:preview": "staticrypt dist/preview -r -d dist --short --remember 30",
    "inject:preview-noindex": "node scripts/inject-preview-noindex.mjs",
    "build:full": "npm run build && npm run encrypt:preview && npm run inject:preview-noindex",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "staticrypt": "^3.5.4"
  }
}
